import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#03a9f4' ,
    },
    secondary: {
      main: '#ff3d00',
    },
    lol: {
      main: '#ffea00',
    },
  },
});

export default theme